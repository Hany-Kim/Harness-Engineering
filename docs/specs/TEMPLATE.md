# Spec: <feature name>

> For features too big for a single plan. Copy to `docs/specs/<slug>.md`.

- **Status:** draft | approved | shipped
- **Plans:** <links to docs/plans/* that implement this>

## Goal
What the user can do when this ships. Success criteria (measurable).

## Non-goals
Explicitly out of scope.

## Design
Data model (types), API surface, state, key flows. Diagrams welcome. Respect the
layering in `docs/architecture/principles.md`.

## Risks & open questions
What could go wrong; what still needs a decision.

## Rollout
Phasing, flags, migrations (forward-only, human-reviewed).
